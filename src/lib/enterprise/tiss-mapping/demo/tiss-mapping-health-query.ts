/** * PoC Application — depende apenas de TISSMappingPort (EPC-21). * * Não é usado por rotas, Server Functions, UI, OCR, IA, Workflow ou Rule Engine. * Serve exclusivamente para provar Application → Port → Adapter → Store. */import type { TISSMappingPort } from "../ports/tiss-mapping-port";import type { TISSMappingCapabilities, TISSMappingHealth } from "../ports/types";export type TISSMappingHealthSummary = {  health: TISSMappingHealth;  capabilities: TISSMappingCapabilities;  architectureLayer: "application";};/** * Query de aplicação: resume saúde/capacidades via Port. * Zero conhecimento de parser XML, validação, regras ou formatos de origem. */export async function getTISSMappingHealthSummary(  port: TISSMappingPort,): Promise<TISSMappingHealthSummary> {  const health = await port.health();  const capabilities = port.capabilities();  return {    health,    capabilities,    architectureLayer: "application",  };}