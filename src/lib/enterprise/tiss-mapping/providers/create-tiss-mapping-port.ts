/** * Provider / factory do TISSMappingPort — inversão de dependência (EPC-21 / FASE 5). * * Application resolve o Port via este factory; nunca instancia adapters * diretamente no Domain. * * Default: DefaultTISSMappingAdapter. */import { createTISSMappingFactory } from "../factory/tiss-mapping-factory";import type { TISSMappingPort } from "../ports/tiss-mapping-port";import type { TISSMappingProviderOptions } from "../ports/types";/** * Cria o TISSMappingPort para o mecanismo solicitado. * * Default de produção da fundação: DefaultTISSMappingAdapter * (store in-process). */export function createTISSMappingPort(options: TISSMappingProviderOptions = {}): TISSMappingPort {  return createTISSMappingFactory().create(options);}