/** * MappingRelationship — representação estrutural (EPC-21 / FASE 6). * * Exemplos estruturais (sem motor / traversal): *   FieldMapping → ConceptMapping → CanonicalMapping * * Somente representação. Sem lógica. Sem validação. Sem regras. */import type {  MappingConfigurationReference,  MappingMetadataReference,  MappingRecordKind,  MappingStatus,  MappingTag,} from "./models";/** * Relacionamento estrutural entre registros de Mapping. * Estrutural apenas — não executa joins, projeção ou workflow. */export type MappingRelationship = {  id: string;  version?: string;  status?: MappingStatus;  metadataReference?: MappingMetadataReference;  configurationReference?: MappingConfigurationReference;  tags?: readonly MappingTag[];  customAttributes?: Readonly<Record<string, unknown>>;  createdAt?: string;  updatedAt?: string;  description?: string;  /** Kind do registro de origem. */  sourceKind: MappingRecordKind;  /** Id do registro de origem. */  sourceMappingId: string;  /** Kind do registro de destino. */  targetKind: MappingRecordKind;  /** Id do registro de destino. */  targetMappingId: string;  /**   * Tipo estrutural livre do relacionamento   * (ex.: "field-to-concept", "concept-to-canonical", "definition-contains").   */  relationshipType?: string;};/** * Cadeia estrutural obrigatória documentada (não executável). * Serve como referência de modelagem — sem runtime. */export const MAPPING_LAYER_CHAIN = [  "field",  "concept",  "canonical",] as const satisfies readonly MappingRecordKind[];/** * Pipeline arquitetural obrigatório (documentação estrutural). */export const MAPPING_PIPELINE = [  "origem",  "field-mapping",  "concept-mapping",  "canonical-mapping",  "healthcare-model",] as const;