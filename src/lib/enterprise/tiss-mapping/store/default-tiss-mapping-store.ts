/** * DefaultTISSMappingStore — store in-process padrão (EPC-21). * * Utiliza mecanismo em memória do processo (sem novo banco, sem migrations). */import type {  StoredCanonicalMapping,  StoredConceptMapping,  StoredFieldMapping,  StoredMappingDefinition,  StoredMappingRecord,  StoredMappingRelationship,  StoredMappingVersion,  TISSMappingStore,} from "./tiss-mapping-store";export const DEFAULT_TISS_MAPPING_STORE_ID = "default-in-process";export type DefaultTISSMappingStoreOptions = {  fieldMappings?: readonly StoredFieldMapping[];  conceptMappings?: readonly StoredConceptMapping[];  canonicalMappings?: readonly StoredCanonicalMapping[];  definitions?: readonly StoredMappingDefinition[];  versions?: readonly StoredMappingVersion[];  relationships?: readonly StoredMappingRelationship[];};export class DefaultTISSMappingStore implements TISSMappingStore {  readonly storeId = DEFAULT_TISS_MAPPING_STORE_ID;  private readonly fieldMappings = new Map<string, StoredFieldMapping>();  private readonly conceptMappings = new Map<string, StoredConceptMapping>();  private readonly canonicalMappings = new Map<string, StoredCanonicalMapping>();  private readonly definitions = new Map<string, StoredMappingDefinition>();  private readonly definitionsByName = new Map<string, string>();  private readonly versions = new Map<string, StoredMappingVersion>();  private readonly relationships = new Map<string, StoredMappingRelationship>();  constructor(options: DefaultTISSMappingStoreOptions = {}) {    for (const mapping of options.fieldMappings ?? []) {      this.setFieldMapping(mapping);    }    for (const mapping of options.conceptMappings ?? []) {      this.setConceptMapping(mapping);    }    for (const mapping of options.canonicalMappings ?? []) {      this.setCanonicalMapping(mapping);    }    for (const definition of options.definitions ?? []) {      this.setDefinition(definition);    }    for (const version of options.versions ?? []) {      this.setVersion(version);    }    for (const relationship of options.relationships ?? []) {      this.relationships.set(relationship.id, relationship);    }  }  getFieldMapping(id: string): StoredFieldMapping | undefined {    return this.fieldMappings.get(id);  }  setFieldMapping(mapping: StoredFieldMapping): void {    this.fieldMappings.set(mapping.id, mapping);  }  listFieldMappings(): readonly StoredFieldMapping[] {    return [...this.fieldMappings.values()];  }  removeFieldMapping(id: string): boolean {    return this.fieldMappings.delete(id);  }  fieldMappingCount(): number {    return this.fieldMappings.size;  }  getConceptMapping(id: string): StoredConceptMapping | undefined {    return this.conceptMappings.get(id);  }  setConceptMapping(mapping: StoredConceptMapping): void {    this.conceptMappings.set(mapping.id, mapping);  }  listConceptMappings(): readonly StoredConceptMapping[] {    return [...this.conceptMappings.values()];  }  removeConceptMapping(id: string): boolean {    return this.conceptMappings.delete(id);  }  conceptMappingCount(): number {    return this.conceptMappings.size;  }  getCanonicalMapping(id: string): StoredCanonicalMapping | undefined {    return this.canonicalMappings.get(id);  }  setCanonicalMapping(mapping: StoredCanonicalMapping): void {    this.canonicalMappings.set(mapping.id, mapping);  }  listCanonicalMappings(): readonly StoredCanonicalMapping[] {    return [...this.canonicalMappings.values()];  }  removeCanonicalMapping(id: string): boolean {    return this.canonicalMappings.delete(id);  }  canonicalMappingCount(): number {    return this.canonicalMappings.size;  }  getDefinition(id: string): StoredMappingDefinition | undefined {    return this.definitions.get(id);  }  getDefinitionByName(name: string): StoredMappingDefinition | undefined {    const id = this.definitionsByName.get(name);    return id ? this.definitions.get(id) : undefined;  }  setDefinition(definition: StoredMappingDefinition): void {    const previous = this.definitions.get(definition.id);    if (previous && previous.name !== definition.name) {      this.definitionsByName.delete(previous.name);    }    this.definitions.set(definition.id, definition);    this.definitionsByName.set(definition.name, definition.id);  }  listDefinitions(): readonly StoredMappingDefinition[] {    return [...this.definitions.values()];  }  removeDefinition(id: string): boolean {    const existing = this.definitions.get(id);    if (!existing) return false;    this.definitionsByName.delete(existing.name);    return this.definitions.delete(id);  }  definitionCount(): number {    return this.definitions.size;  }  getVersion(id: string): StoredMappingVersion | undefined {    return this.versions.get(id);  }  setVersion(version: StoredMappingVersion): void {    this.versions.set(version.id, version);  }  listVersions(): readonly StoredMappingVersion[] {    return [...this.versions.values()];  }  removeVersion(id: string): boolean {    return this.versions.delete(id);  }  versionCount(): number {    return this.versions.size;  }  getRelationship(id: string): StoredMappingRelationship | undefined {    return this.relationships.get(id);  }  setRelationship(relationship: StoredMappingRelationship): void {    this.relationships.set(relationship.id, relationship);  }  listRelationships(): readonly StoredMappingRelationship[] {    return [...this.relationships.values()];  }  removeRelationship(id: string): boolean {    return this.relationships.delete(id);  }  relationshipCount(): number {    return this.relationships.size;  }  getRecord(id: string): StoredMappingRecord | undefined {    return (      this.fieldMappings.get(id) ??      this.conceptMappings.get(id) ??      this.canonicalMappings.get(id) ??      this.definitions.get(id) ??      this.versions.get(id)    );  }  listRecords(): readonly StoredMappingRecord[] {    return [      ...this.fieldMappings.values(),      ...this.conceptMappings.values(),      ...this.canonicalMappings.values(),      ...this.definitions.values(),      ...this.versions.values(),    ];  }  health(): { ok: boolean; message?: string } {    return {      ok: true,      message: `DefaultTISSMappingStore ready (${this.fieldMappings.size} field, ${this.conceptMappings.size} concept, ${this.canonicalMappings.size} canonical, ${this.definitions.size} definitions, ${this.versions.size} versions, ${this.relationships.size} relationships).`,    };  }}